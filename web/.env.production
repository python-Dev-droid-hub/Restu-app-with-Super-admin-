# Production web — browser calls /api on :5175; serve-prod.mjs proxies to the API.
VITE_API_URL=/api
# Bare-metal VPS: API on same host
VITE_PROXY_TARGET=http://127.0.0.1:3101
# Docker Compose: use http://server:3101 (set in docker-compose.yml)
HOST=0.0.0.0
PORT=5175
