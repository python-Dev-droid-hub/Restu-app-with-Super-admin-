# API server (Node) — must be port 3101, NOT the Vite web port
VITE_PROXY_TARGET=http://127.0.0.1:3101

# Leave unset in dev to use Vite proxy (/api → 3101). Do not set to :8080 or :5173.
# VITE_API_URL=http://127.0.0.1:3101/api

VITE_APP_NAME=Restaurant App
VITE_APP_ENV=development
