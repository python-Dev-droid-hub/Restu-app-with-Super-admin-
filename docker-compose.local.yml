services:
  mongo:
    ports:
      - "27019:27017"

  server:
    environment:
      NODE_ENV: development
      PORT: "3101"
      CORS_ORIGIN: http://localhost:5175
      COOKIE_SECURE: "false"
      COOKIE_SAMESITE: lax

  web:
    build:
      args:
        VITE_API_URL: http://localhost:3101/api
