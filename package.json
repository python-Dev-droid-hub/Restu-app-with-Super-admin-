{
  "name": "restaurant-app",
  "private": true,
  "scripts": {
    "dev": "npx concurrently -n server,web -c blue,green \"npm run dev --prefix server\" \"npx wait-on http://127.0.0.1:3101/health -t 120000 && npm run dev --prefix web\"",
    "dev:server": "npm run dev --prefix server",
    "dev:web": "npm run dev --prefix web"
  },
  "dependencies": {
    "react-native-safe-area-context": "^5.6.2"
  }
}
